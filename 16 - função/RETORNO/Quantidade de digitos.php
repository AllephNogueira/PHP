<?php

    // Calcular a soma dos digitos
    // Exemplo 20 = tem 2 digitos
    
    function Digitos ($numero){

        $digitos = strlen($numero);  // Pegando a quantidade de digitos 

        return $digitos;

        // Agora vamos dividir os digitos e somar eles

        

    }

    echo "quantidade de digitos: ".Digitos(50000);